/**
 * Константы для временных задержек и таймаутов
 * Используются вместо магических чисел в setTimeout/задержках
 */
export const DELAYS = {
  // Минимальная задержка для отложенных обновлений DOM (один тик event loop)
  // Используется для updateXarrow когда нужно дождаться обновления DOM
  IMMEDIATE: 0,
  // Задержка для позиционирования элементов после рендера
  POSITIONING: 50,
  // Задержка для обновления стрелок после изменений DOM
  ARROW_UPDATE: 100,
  // Задержка для восстановления связей после drag-and-drop
  LINK_RESTORATION: 100,
  // Задержка для прокрутки к элементу (одна попытка)
  SCROLL_ATTEMPT: 150,
  // Задержка для UI обновлений (обновление целей после изменения)
  UI_UPDATE: 200,
  // Задержка для дебаунсинга API запросов (сохранение позиций/связей)
  DEBOUNCE: 300,
  // Задержка для ожидания создания задачи в API
  TASK_CREATION: 500,
  // Задержка для начальной прокрутки после монтирования
  INITIAL_SCROLL: 500,
  // Максимальное количество попыток для повторных операций (прокрутка к элементу)
  MAX_RETRIES: 20,
} as const;
