/** HttpOnly cookie с подписанной сессией продукта (users.id). */
export const PRODUCT_SESSION_COOKIE_NAME = 'bt_product_session';

/** Срок жизни сессии (секунды). */
export const PRODUCT_SESSION_MAX_AGE_SEC = 60 * 60 * 24 * 14;
